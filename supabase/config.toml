# Minimal Supabase config — run `supabase link` (writes your real project ref locally).
# Use a placeholder in git; never commit production project IDs or secrets.
project_id = "localdevplaceholder01"

[api]
enabled = true

[db]
major_version = 17

[auth]
enabled = true
